# DigiiMark Voice AI Agent — Python dependencies (web/server mode).
#
# These are everything needed to run `python server.py` on Railway / a VPS.
# Terminal mode additionally requires PyAudio — install it separately via
# `pip install pyaudio` after system dependencies (PortAudio) are in place.

# Gemini Live API + embeddings + function calling
google-genai>=1.0.0

# Supabase client (RAG retrieval, transcripts, content lookups)
supabase>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# Async HTTP for tool webhooks (Cal.com, lead capture)
httpx>=0.27.0

# Rich terminal output (logs + admin CLI)
rich>=13.0.0

# FastAPI web server for the browser widget
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Numerical types (used indirectly by pgvector serialisation and local tests)
numpy>=1.26.0
